<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sys.slave.TestSlaveDao">
    <select id="getUser" resultType="map">
        select * from buser b where b.loginname='sys'
    </select>
    <update id="upds">
        update buser b set b.name='77777777' where b.loginname='sys'
    </update>
</mapper>